class Solution:
  def solve(self, board: list[list[str]]) -> None:
    if not board:
      return

    DIRS = ((0, 1), (1, 0), (0, -1), (-1, 0))
    m = len(board)
    n = len(board[0])
    q = collections.deque()

  
         
      

